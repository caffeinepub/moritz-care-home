{
  "schemaVersion": "1.0.0",
  "projectStateVersion": 3,
  "summary": "Project now includes fixing a startup/connectivity issue where the dashboard briefly appears then gets stuck on \"Connecting to backend...\"; user requested a code change to force faster fallback when backend is slow but reachable.",
  "architectural_notes": [
    "Add startup connection timeout + fallback path (error/retry screen) to prevent indefinite 'Connecting to backend' state when backend actor/profile load is slow."
  ],
  "known_issues": [
    "Dashboard flashes empty then becomes stuck on 'Connecting to backend' (startup/actor/profile load can stall indefinitely when backend is slow)."
  ],
  "isFixRequest": true,
  "existing_features": [
    {
      "id": "feature-update-startup-connection-logic-to-fail-fast-show-startuperrorscreen-when-the-backend-is-reachable-but-actor-creation-or-profile-loading-is-slow-instead-of-staying-on-the-infinite-connecting-to-backend-loading-your-profile-screens-until-the-full-45s-watchdog-expires",
      "title": "Update startup connection logic to fail fast (show StartupErrorScreen) when the backend is reachable but actor creation or profile loading is slow, instead of staying on the infinite \"Connecting to backend...\" / \"Loading your profile...\" screens until the full 45s watchdog expires.",
      "reqIds": [
        "REQ-1"
      ],
      "version": 1
    },
    {
      "id": "feature-add-explicit-per-step-timeouts-to-startup-calls-that-can-hang-even-when-the-backend-is-reachable-including-the-startup-profile-fetch-getcalleruserprofile-and-any-other-startup-critical-requests-using-the-existing-timeout-utility-withtimeout-so-they-error-quickly-and-trigger-the-existing-error-ui-flow",
      "title": "Add explicit per-step timeouts to startup calls that can hang even when the backend is reachable, including the startup profile fetch (getCallerUserProfile) and any other startup-critical requests, using the existing timeout utility (withTimeout) so they error quickly and trigger the existing error UI flow.",
      "reqIds": [
        "REQ-2"
      ],
      "version": 1
    },
    {
      "id": "feature-run-performhealthcheck-earlier-during-slow-startup-not-only-after-an-error-timeout-so-the-ui-can-distinguish-backend-unreachable-vs-backend-reachable-but-slow-and-present-the-faster-fallback-accordingly",
      "title": "Run performHealthCheck earlier during slow startup (not only after an error/timeout), so the UI can distinguish \"backend unreachable\" vs \"backend reachable but slow\" and present the faster fallback accordingly.",
      "reqIds": [
        "REQ-3"
      ],
      "version": 1
    }
  ],
  "new_feature_requests": [
    {
      "id": "AR-1",
      "summary": "Implement faster startup fallback/timeout so UI doesn’t get stuck on 'Connecting to backend' when backend is slow but reachable (improve retry/error handling).",
      "reqIds": [],
      "status": "in_progress"
    }
  ],
  "projectName": "Force faster startup fallback when backend is reachable but slow",
  "clarification_mode": "instant",
  "clarification_rounds": 0
}