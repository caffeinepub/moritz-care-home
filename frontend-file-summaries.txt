{
  "frontend/src/App.tsx": "Main application component with a phase-based startup flow (initializing → connecting → loading-profile → dashboard/profile-setup/error) that includes an overall watchdog timer, a dedicated profile-loading timeout guard, proper error surfacing with retry UI, and guards preventing circular redirects between profile-setup and dashboard.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; useGetCallerUserProfile now correctly calls getUserProfileWithRole with the caller's principal, handles the result type (ok/err/NotFound), includes a per-query timeout, and has retry:2 with staleTime:30000 to prevent indefinite hangs.",
  "frontend/src/pages/LoginPage.tsx": "Login page with Internet Identity authentication, fully opaque solid backgrounds throughout including card and completely opaque teal overlay on background image, with Moritz Care Home logo displayed in rectangular mode above the title for proper aspect ratio.",
  "frontend/src/pages/Dashboard.tsx": "Main dashboard importing `Resident` from `../hooks/useQueries` (local type) and using `AddResidentDialog` with children/trigger pattern instead of `open/onOpenChange` props.",
  "frontend/src/pages/ResidentProfile.tsx": "Resident profile page passing `Number(residentId)` to `useGetResident`, using all dialog components with the children/trigger pattern, and calling `formatDate`/`calculateAge` with `number` timestamps from the local `Resident` type.",
  "frontend/src/components/ProfileSetup.tsx": "User profile setup component for first-time login, updated to import `useSaveCallerUserProfile` (renamed from the removed `useSaveCallerUserProfileStartup`), with name/employeeId form, error display, and automatic transition on successful save.",
  "frontend/src/components/AddResidentDialog.tsx": "Self-contained dialog for adding new residents using `dateToBackendTimestamp` (correct export name), with internal open/close state managed via `DialogTrigger` instead of external `open/onOpenChange` props.",
  "frontend/src/components/EditResidentDialog.tsx": "Dialog for editing resident information importing `Resident` from `../hooks/useQueries`, using `backendTimestampToDate` (accepts `bigint | number`) for pre-filling date fields, with internal open/close state via `DialogTrigger`.",
  "frontend/src/components/AddMarRecordDialog.tsx": "Dialog form for adding medication administration records with medication selection dropdown and solid white backgrounds for all UI elements including dialog content, inputs, selects, textareas, buttons, and cards.",
  "frontend/src/components/AddAdlRecordDialog.tsx": "Dialog form for recording activities of daily living with predefined activity types and assistance levels, using solid white backgrounds for all UI elements including dialog content, inputs, selects, textareas, and buttons.",
  "frontend/src/index.css": "Global styles with comprehensive solid white dialog enforcement, healthcare-themed OKLCH color system, and enhanced print styles with fixed-width desktop layout override (1000px) that forces consistent multi-column rendering across all devices including mobile and tablet for Resident Profile Report, MAR Report, and ADL Report.",
  "frontend/src/components/AddMedicationDialog.tsx": "Dialog component for adding new medications to a resident with solid white backgrounds for all UI elements including dialog content, inputs, selects, textareas, and buttons.",
  "frontend/src/lib/dateUtils.ts": "Date utility functions updated so `formatDate`, `calculateAge`, `backendTimestampToDate`, and `nanosecondsToDateString` all accept `bigint | number` to be compatible with the local `Resident` type that uses `number` timestamps; also adds `backendTimestampToDateString` alias.",
  "frontend/src/components/EditMedicationDialog.tsx": "Dialog component for editing existing medications using useUpdateMedication hook, with pre-filled form fields, status selection, and proper error handling with toast notifications.",
  "frontend/tailwind.config.js": "Tailwind configuration with healthcare theme colors using fully opaque OKLCH system and solid boxShadow definitions without any alpha transparency values.",
  "frontend/src/components/AddDailyVitalsDialog.tsx": "Dialog component for recording daily vital signs with solid white backgrounds for all UI elements including dialog content, inputs, selects, textareas, and buttons.",
  "frontend/src/components/AddWeightEntryDialog.tsx": "Dialog for recording weight entries importing `Resident` from `../hooks/useQueries` (local type) instead of `../backend`, passing `measurementDate` as `number` (milliseconds × 1_000_000) to match the mutation signature.",
  "frontend/src/hooks/useActor.ts": "Actor initialization hook that skips the `_initializeAccessControlWithSecret` update call when no admin token is present, eliminating the ~2s IC update call overhead on every login.",
  "frontend/src/components/StartupErrorScreen.tsx": "Full-screen error component for startup failures accepting `Error | null` for the error prop and `HealthCheckResult | null` for health check result, showing backend diagnostics (canister ID, host, environment), context-specific guidance for stopped/network/not-found errors, and retry/logout buttons.",
  "frontend/src/lib/actorInit.ts": "Actor initialization utilities with timeout wrapper, error normalization, and classification helpers for stopped canister, canister not found, network errors, and timeout errors to support consistent user-facing messaging.",
  "frontend/src/hooks/useResilientActor.ts": "Resilient actor initialization hook using `withTimeout` from `actorInit` and renamed timing constants (`ACTOR_CREATION_TIMEOUT`, `ADMIN_INIT_TIMEOUT`) from `startupTimings`, skipping admin init when no token is present.",
  "frontend/src/lib/assetUrl.ts": "Utility function for constructing base-path-safe URLs for public assets with hardened path normalization to prevent double slashes and ensure assets work correctly in both local development and non-root deployments.",
  "frontend/src/components/BrandLogo.tsx": "Reusable logo component that renders the Moritz Care Home logo from the uploaded MoritzCareHomeLOGO-1.png asset with support for both square and rectangular modes, automatic fallback to clean text display when image fails to load, and configurable sizing via props with proper aspect ratio preservation for non-square logos.",
  "frontend/DEPLOYMENT_VERSION_89.md": "Historical Version 89 deployment checklist with prominent note linking to current DEPLOYMENT_CHECKLIST.md for production deployments.",
  "frontend/src/lib/startupDiagnostics.ts": "Startup diagnostics utilities that export `getBackendDiagnostics` (reads `VITE_CANISTER_ID_BACKEND` with fallbacks), `getBackendEnvInfo` (alias returning canisterId+host), and `performHealthCheck` (anonymous actor health check with 5s timeout), plus the full `HealthCheckResult` type with `status`, `durationMs`, and `success` fields required by App.tsx, DiagnosticsIndicator, and StartupErrorScreen.",
  "frontend/src/lib/startupTimings.ts": "Centralized startup timing configuration with production-optimized values including a new PROFILE_QUERY_TIMEOUT constant (12000ms) for bounding the profile loading phase.",
  "frontend/scripts/deploy_version_89_publish_only_retry.sh": "Historical Version 89 deployment script with deprecation header pointing operators to redeploy_production.sh for current deployments.",
  "frontend/SMOKE_TEST_VERSION_89.md": "Comprehensive post-deployment smoke test checklist with explicit \"Connecting to backend...\" hang detection (10-second observation), structured failure-report section capturing deployment log/canister ID file paths, and dedicated diagnostics indicator verification step cross-checking backend canister ID against deployment artifacts.",
  "frontend/src/lib/buildInfo.ts": "Build information utility that safely reads Vite-injected build metadata constants (__BUILD_ID__, __BUILD_TIME__, __GIT_COMMIT__) with try/catch guards, returning a BuildInfo object with a hasMetadata flag.",
  "frontend/src/components/DiagnosticsIndicator.tsx": "Compact diagnostics indicator button showing backend canister ID and environment with a tooltip containing full diagnostic details including build info, using warning/success icons based on configuration completeness.",
  "frontend/deployment_logs/.gitkeep": "Documentation for deployment logs directory explaining that both redeploy_production.sh and clean_redeploy_production.sh write timestamped deploy logs and canister ID summary files, with details on when to use the clean rebuild script and what additional steps it performs.",
  "frontend/src/lib/residentEnumMapping.ts": "Utility functions to map between shadcn/ui Select string values and backend enum types (RoomType, ResidentStatus) with added backward-compatible aliases (mapRoomTypeToBackend, mapRoomTypeFromBackend, mapResidentStatusFromBackend).",
  "frontend/scripts/redeploy_production.sh": "Production frontend redeploy script that extracts the backend canister ID from `canister_ids.json` (or `dfx canister id`) and writes it to `frontend/.env` as `VITE_CANISTER_ID_BACKEND` **before** the Vite build, ensuring the canister ID is baked into the production bundle.",
  "frontend/DEPLOYMENT_CHECKLIST.md": "Comprehensive deployment checklist with three deployment options (manual, standard redeploy, clean rebuild), dedicated backend canister management section with status checking and start/restart instructions using the restart script, explicit guidance on when to use each deployment method, and detailed troubleshooting for stopped canister scenarios.",
  "frontend/scripts/post_deploy_smoke_test_helper.sh": "Interactive smoke test helper script that guides operators through documented smoke tests, verifies deployment artifacts (canister IDs, build metadata, repository state), checks for \"Connecting to backend...\" hang (10-second observation), validates diagnostics indicator values against deployment artifacts, and confirms hard refresh stability.",
  "frontend/scripts/clean_redeploy_production.sh": "Clean rebuild + production redeploy script that removes prior build artifacts, then extracts the backend canister ID and writes it to `frontend/.env` before building, ensuring a correctly configured bundle with `VITE_CANISTER_ID_BACKEND` set.",
  "frontend/src/backend.d.ts": "Updated TypeScript interface for backend canister with new postLoginInitialization() method that returns the caller's role as English text, enabling one-time admin bootstrap on first authenticated login.",
  "frontend/scripts/restart_backend_canister.sh": "Operator script that checks backend canister status on IC network and starts it if stopped, with support for forced restart mode (stop+start), comprehensive logging, and error handling with non-zero exit codes on failure.",
  "frontend/src/utils/urlParams.ts": "Utility function for extracting URL query parameters, specifically for retrieving secret tokens (admin and user tokens) needed for backend initialization.",
  "frontend/src/lib/registrationErrorMapping.ts": "Utility module that maps backend registration/bootstrap errors into stable, user-accurate English messages, distinguishing between anonymous/unauthenticated access, admin-only provisioning flows, and generic failures without defaulting to the legacy \"contact administrator\" message for normal authenticated users."
}